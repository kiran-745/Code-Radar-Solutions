#include <stdio.h>

int main() {
    int num1, num2, sum;

    scanf("%d %d", &num1, &num2);

    sum = num1 + num2;

    printf("Sum: %d\n", sum); // Print "Sum: " followed by the sum and a newline

    return 0;
}