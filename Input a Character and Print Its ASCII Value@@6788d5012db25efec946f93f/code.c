#include <stdio.h>

int main() {
    char ch;

    scanf("%c", &ch); // Read the character

    printf("ASCII Value: %d\n", ch); // Print "ASCII Value: " and then the ASCII value

    return 0;
}