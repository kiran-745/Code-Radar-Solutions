#include <stdio.h>

int main() {
    int num;

    scanf("%d", &num); // Read an integer using %d format specifier

    printf("%d\n", num); // Print the integer followed by a newline

    return 0;
}